name: green_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  dartz: ^0.10.1
  
  # CORREGIDO: Provider ahora está alineado correctamente a la izquierda
  provider: ^6.1.5+1

  # --- UI ---
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # --- State Management ---
  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  
  # --- Dependency Injection ---
  get_it: ^7.7.0
  injectable: ^2.4.2

  # --- Stream Transform ---
  stream_transform: ^2.1.0

  # --- Functional Programming & Utils ---
  rxdart: ^0.27.7

  # --- Networking & Data ---
  dio: ^5.9.0
  http: ^1.2.0
  json_annotation: ^4.9.0
  shared_preferences: ^2.3.2
  fpdart: ^1.2.0
  intl: ^0.20.2
  flutter_secure_storage: ^10.0.0

  # --- Multimedia & Files ---
  image_picker: ^1.0.4
  path_provider: ^2.1.0
  path: ^1.8.3
  
  # Usa una versión más estable de file_picker
  # file_picker: ^5.3.3  # Versión más estable (comenta si no es esencial)
  
  # Alternativa más ligera para selección de archivos
  file_selector: ^0.9.3+1  # Alternativa oficial de Flutter

  mobile_scanner: ^7.1.4

  socket_io_client: ^3.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # --- Code Generation ---
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
  injectable_generator: ^2.6.1
  mocktail: ^1.0.3

flutter:
  uses-material-design: true

  assets:
    - assets/appIcon.png