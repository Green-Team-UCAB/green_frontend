name: green_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # CORREGIDO: Provider ahora está alineado correctamente a la izquierda
  provider: ^6.1.2 # (Ajusté la versión a una estándar estable, verifica si necesitas la 6.1.5+1 específica)

  # --- UI ---
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # --- State Management ---
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  
  # --- Dependency Injection ---
  get_it: ^7.7.0
  injectable: ^2.4.2

  # --- Functional Programming & Utils ---
  rxdart: ^0.27.7

  # --- Networking & Data ---
  dio: ^5.5.0+1
  json_annotation: ^4.9.0
  shared_preferences: ^2.2.3
  fpdart: ^1.2.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # --- Code Generation ---
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
  injectable_generator: ^2.6.1
  mocktail: ^1.0.3

flutter:
  uses-material-design: true