name: green_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # --- UI ---
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # --- State Management ---
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  
  # --- Dependency Injection ---
  get_it: ^7.7.0
  injectable: ^2.4.2

  # --- Functional Programming & Utils ---
  dartz: ^0.10.1
  rxdart: ^0.27.7 # Necesario para debounceTime en el buscador

  # --- Networking & Data ---
  dio: ^5.5.0+1
  json_annotation: ^4.9.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
  # --- Code Generation ---
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
  injectable_generator: ^2.6.1
  mocktail: ^1.0.3

flutter:
  uses-material-design: true
  
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg